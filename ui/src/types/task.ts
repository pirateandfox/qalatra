// Re-exported from @qalatra/shared so existing `./types/task` imports keep
// working. The definitions now live in packages/shared/src/types.
export type { Task, RelatedTask, Attachment, Subtask, Note } from '@qalatra/shared'
